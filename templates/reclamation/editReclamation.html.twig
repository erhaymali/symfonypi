{% extends 'base1.html.twig' %}

{% block body %}
    <div style="display: flex; align-items: center; justify-content: center; height: 100vh;">
        <div class="reclamation-form" style="max-width: 600px;"> {# Adjust the max-width as needed #}
            <h1>Edit Reclamation</h1>

            {{ form_start(form) }}

            {# Form fields here... #}

            <div class="form-group">
                {{ form_label(form.dateRec, 'Date', {'label_attr': {'class': 'custom-label'}}) }}
                {{ form_widget(form.dateRec, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="form-group">
                {{ form_label(form.sujet, 'Sujet', {'label_attr': {'class': 'custom-label'}}) }}
                {{ form_widget(form.sujet, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="form-group">
                {{ form_label(form.contenu, 'Contenu', {'label_attr': {'class': 'custom-label'}}) }}
                {{ form_widget(form.contenu, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="form-group">
                {{ form_label(form.etat, 'État', {'label_attr': {'class': 'custom-label'}}) }}
                {{ form_widget(form.etat, {'attr': {'class': 'form-control'}}) }}
            </div>
            <br/>
            <br/>

            <div class="form-group text-center">

                <button type="submit" class="btn btn-primary btn-lg">Submit</button>
            </div>

            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
